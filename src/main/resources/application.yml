spring:
  application:
    name: microservice

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Aniks68/Microservice-Project.git
#          /* TODO: switch URL to properties repo */


server:
  port: ${port:8888}